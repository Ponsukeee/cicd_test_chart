apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cicd_test_chart.fullname" . }}-test-connection"
  labels:
{{ include "cicd_test_chart.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cicd_test_chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
